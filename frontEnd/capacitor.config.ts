import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elgransabor.app',
  appName: 'El Gran Sabor',
  webDir: 'dist/frontend/browser'
};

export default config;
